'use strict';

// Object.prototype.hashMap = (self, callback) => {
//     return Object.keys(self).map(key => {
//         return callback(self[key], key);
//     });
// };

// Object.prototype.hashFilter = (self, callback) => {
//     return Object.keys(self).filter(key => {
//         return callback(self[key], key);
//     });
// };

module.exports = {};
