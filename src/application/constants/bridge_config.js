const gasTokenPrice = 20;

const min_send_token = gasTokenPrice + 80;
const max_send_token = 100000000000000000000000000;

module.exports = {
    gasTokenPrice,
    min_send_token,
    max_send_token,
};
